# Cloudflare Pages SPA 路由 + 代理转发配置
# 格式：source destination status_code
# 200! = 代理转发（不改变浏览器 URL），200 = 重定向

# SPA 路由：所有非 /api/ 请求走 index.html
/*    /index.html   200

# GitHub API 代理和 SearXNG 代理由 functions/ 目录处理
# Cloudflare Pages 会自动将 /api/* 路由到 functions/api/*
